# Play Internal — v0.1.403 (versionCode 403)

**Fix:** usuarios registrados visibles en Explorar (discovery multi-ciudad + ciudad canónica en Firestore).

## What's new (ES)

```
v0.1.403 — Fix ver personas registradas en Explorar
• Consulta todas las variantes de ciudad (Viña, Concón, legacy)
• Normaliza ciudad al guardar perfil en Firestore
• Listener tiempo real unificado con fetch de discovery
```

## Comando

```powershell
cd C:\Users\muscl\fitvina
.\publish-play.bat internal
```
