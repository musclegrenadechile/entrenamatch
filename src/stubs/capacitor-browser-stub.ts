/** Web/dev stub — @capacitor/browser is only required in native builds. */
export const Browser = {
  open: async () => {},
  close: async () => {},
  addListener: async () => ({ remove: async () => {} }),
  removeAllListeners: async () => {},
}
