/** Web/dev stub — @capacitor/app is only required in native builds. */
export const App = {
  addListener: async () => ({ remove: async () => {} }),
  removeAllListeners: async () => {},
  exitApp: async () => {},
}
