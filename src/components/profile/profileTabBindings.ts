import type { ProfileTabProps } from './profileTabTypes'

/** Shared destructuring for profile sub-sections (avoids repeating 100+ lines). */
export function profileTabBindings(props: ProfileTabProps) {
  return props
}
