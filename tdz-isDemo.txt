fix(TDZ): hoist useAuth/useDemoAuth very early (right after useProfile) so isDemoMode and firebaseUser are available before early effects like the daily offline effect (~1188) that have them in deps. This eliminates the TDZ for isDemoMode (likely the persisting " Cannot access z before initialization\ on open, as the dep array evaluation was hitting uninitialized binding). Removed the duplicate late destructure. tsc clean.
