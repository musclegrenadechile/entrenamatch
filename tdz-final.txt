fix(TDZ): also hoist deck, visibleCards, chatProfile, matchProfiles early with the other discovery memos. Now the entire live/pulse/discovery layer (remaining, live, zone, feed, deck, match etc) is declared right after real multi-user state (~1460). All dependent effects and JSX are after. Combined with daily ultra-early hoist, this eliminates remaining forward references in render order for the vars that were causing " Cannot access z before initialization\ on open. tsc clean. Rebuild required for bundle.
